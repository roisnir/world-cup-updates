# Secrets — never commit
.env

# Python
__pycache__/
*.pyc

# Local run artifacts
wc.log
results.json

# WhatsApp (Baileys) bridge — node deps and the linked-device credentials.
# auth/ is as sensitive as a password: anyone with it can post as your account.
whatsapp/node_modules/
whatsapp/auth/
